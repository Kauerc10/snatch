{
  "name": "snatch-mvp",
  "version": "0.3.0",
  "private": true,
  "description": "Playable SNATCH! Daily Heist MVP with deterministic physical claw game feel",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "python3 tests/browser_smoke.py && python3 tests/browser_interaction.py && python3 tests/risk_hud.py && python3 tests/review_regressions.py",
    "test:all": "npm test && npm run test:browser",
    "serve": "python3 -m http.server 4173"
  }
}
