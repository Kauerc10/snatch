{
  "name": "snatch-mvp",
  "version": "0.2.0",
  "private": true,
  "description": "Playable SNATCH! daily-heist MVP with risk/reward polish",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "serve": "python3 -m http.server 4173",
    "test:browser": "python3 tests/browser_smoke.py && python3 tests/browser_interaction.py && python3 tests/risk_hud.py"
  }
}
