{
  "name": "snatch-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Playable greybox MVP for SNATCH! — One more?",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "serve": "python3 -m http.server 4173"
  }
}
